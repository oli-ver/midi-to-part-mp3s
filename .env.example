# Domain served by the nginx-proxy reverse proxy
VIRTUAL_HOST=your.host.name

# Domain for which Let's Encrypt certificates are requested
LETSENCRYPT_HOST=your.host.name

# Send Python stdout/stderr straight to the terminal (no buffering)
PYTHONUNBUFFERED=1
