version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/webapp"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    # Only monitor direct dependencies by default; adjust if you want indirect updates
    allow:
      - dependency-type: "direct"
